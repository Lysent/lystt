class GamestateClient {
	constructor(map) {
		this.map = map;
	};

	setMap(mapobj) {
		this.map = mapobj;
	};

	updateMap(mapdiff) {

	};

	deserializeMap(mapnetpak) {

	};
};

export { GamestateClient };